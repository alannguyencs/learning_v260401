# Sample size analysis

Before running, you compute how many users each group needs so that, given your α, target lift, and baseline rate, the test is trustworthy. Too small → noisy and unreliable; too large → wasted traffic and time.
