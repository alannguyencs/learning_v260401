# Projection matrix (W_Q, W_K, W_V)

Three learned weight matrices. Multiplying a token's embedding by each one produces its query, key, and value vector respectively. "Learned" = adjusted during training by gradient descent.
