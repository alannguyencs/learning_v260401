# Key (k)

`k = W_K · embedding`. The "what do I offer, for matching?" vector. Attention compares queries against keys. *Simple example:* like the **topic label on each book's spine** ("Astronomy", "Cooking") that the catalog checks your search against.
