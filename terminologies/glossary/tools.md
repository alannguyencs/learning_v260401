# Tools

*(stay in the MCP Server)* — **Model-controlled** executable functions the server exposes (e.g. "list my repositories"). The server ships **both** the schema and the implementation and **runs** them — tools live and execute on the **MCP Server side**. The LLM only decides when to call one; the MCP Client merely relays that call.
