# RBAC (Role-Based Access Control)

Decide from a **label granted to the user ahead of time**: bundle permissions into **roles** (`admin`, `editor`, `viewer`), assign roles to users, allow if the user holds a role that grants the action.
