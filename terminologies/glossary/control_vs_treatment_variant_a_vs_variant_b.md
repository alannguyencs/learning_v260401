# Control vs. treatment (variant A vs. variant B)

The **control** is the existing version users already see; the **treatment** is the new version with exactly one thing changed (a headline, a button color, a checkout flow). Keeping everything else identical is what lets you attribute any behavior difference to that one change.
