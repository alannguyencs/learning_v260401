# Model routing (model selection)

Inspecting each query and **sending easy ones to the cheap model, hard ones to the premium model** (sometimes letting the cheap model answer first and only escalating if a verifier isn't satisfied). *Example:* The receptionist reads the question, and if it's simple, sends it to the cheap teaching assistant; only the genuinely hard ones get escalated to the expensive professor.
