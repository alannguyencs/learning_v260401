# Query (q)

`q = W_Q · embedding`. The "what am I looking for?" vector for the token currently doing the looking. *Simple example:* like typing **"books about space"** into a library catalog — it's the request this token is making.
