# Path / graph traversal

A connected sequence of edges from one node to another (`alan → manages → chloe → member_of → ProjectAlpha → contains → Task#42`). Traversal is walking those edges to discover whether a path exists — the same idea as BFS/DFS you saw in a data-structures course.
